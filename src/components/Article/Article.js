import React from 'react';

const Articles = (props) => {
    const { article } = props;
    console.log(article)
    return (
        <div>
            {
                <h1>Hello</h1>
            }
        </div>
    );
};

export default Articles;